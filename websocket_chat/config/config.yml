# config/config.yml
production:
  websocket:
    host: "localhost"
    port: 8080

development:
  websocket:
    host: "localhost"
    port: 8080

test:
  websocket:
    host: "localhost"
    port: 8080
